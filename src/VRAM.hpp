#ifndef __VRAM_HPP__
#define __VRAM_HPP__

#include <stdint.h>

/*
class VRAM{
	public:
		VRAM();
		~VRAM();

		
		void writeUpperHWord(int32_t address, int16_t hword);
		void writeLowerHWord(int32_t address, int16_t hword);
		void writeWord(int32_t address, int32_t word);
		void writeDWord(int32_t address, int64_t dword);
		
		int16_t readUpperHWord(int32_t address);
		int16_t readLowerHWord(int32_t address);
		int32_t readWord(int32_t address);
		Instruction readInstruction(int32_t address);
		int64_t readDWord(int32_t address);


	private:	
		



};

class VRAMword{
	public:
		int32_t word;
		Instruction* instruction;
		bool isInstruction;
		bool isData;
}


*/

#endif












